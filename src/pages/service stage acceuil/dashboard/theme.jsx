// src/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#4caf50', // couleur verte
    },
    secondary: {
      main: '#ff5722',
    },
  },
});

export default theme;
